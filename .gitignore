.idea
.mcp.json

.env

/vendor
.phpunit.result.cache
.php-cs-fixer.cache

# demo/storage and demo/bootstrap/cache keep a .gitignore of their own in every
# directory that has to exist but stay empty, the way a Laravel skeleton does. Listing
# them here as well would only add a second place to keep in step.
